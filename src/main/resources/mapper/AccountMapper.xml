<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.login.dao.AccountDao">

	<select id="findByUsername" resultType="com.login.pojo.Account">
	select * from u_account where username=#{username}
	</select>

	<select id="findPasswordByU" resultType="com.login.pojo.Account">
		select passwd from u_account where username=#{username}

	</select>
	<select id="findByUserInfo" resultType="com.login.pojo.Account">
		select ac.* from u_info as ui right join u_account as ac on ui.id=ac.aid where info->'$.name'=#{username} and info->'$.sex'=#{sex} and info->'$.age'=#{age}
	</select>

</mapper>